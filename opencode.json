{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "sccs": {
      "template": "1. Run git status and git diff to summarize current status\n2. Check if there's a CHANGELOG.md and update it with changes\n3. Create a git commit with an appropriate message\n4. Save the current session",
      "description": "Summary current status, update changelog, git commit, save session"
    }
  }
}
